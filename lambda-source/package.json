{
  "name": "pardot-form-handler",
  "version": "1.0.0",
  "description": "Lambda function to validate captcha for pardot form handlers",
  "main": "index.js",
  "scripts": {
    "zip": "mkdir -p ../dist && zip -r -j ../dist/${npm_package_name}.zip index.js"
  },
  "keywords": [],
  "author": "Jay DeLuca",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1"
  }
}
